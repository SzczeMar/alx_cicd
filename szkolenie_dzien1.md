skrót: ctrl + l - czysci ekran w git bash

git log --oneline --reflog
git commit --amend -m "imie psa" plik.txt - można wykonać commit bez git add
git commit -a -m "uzywam opcji -a, która dodaje do commitu wszytskie pliki modyfikowane, ale nie dodaje nowych"

git status -u - pokazuje pliki nieśledzone
git nie pokazuje pustych katalogów i nie da się ich dodać do git
